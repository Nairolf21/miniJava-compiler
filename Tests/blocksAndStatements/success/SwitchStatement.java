public class SwitchStatement {
	void foo() {
		switch (i) {}
		switch (i) {
			case 1 :
		}
		switch (i) {
			case j :
		}
		switch (i) {
			default :
		}
		switch (i) {
			case 1 : case j :
		}
		switch (i) {
			case 1 :
				;
		}
		switch (i) {
			case j :
				;
		}
		switch (i) {
			default :
				;
		}
		switch (i) {
			case 1 : case j :
				;
		}
		switch (i) {
			case 1 :
				;
			case j :
		}
		switch (i) {
			case j :
				;
			case 1 :
		}
		switch (i) {
			case 1 :
				;
			default :
		}
		switch (i) {
			case 1 : case j :
				;
			default :
				;
		}
				switch (i) {
			case 1 :
				;
			case j :
				;
		}
		switch (i) {
			case j :
				;
			case 1 :
				;
		}
		switch (i) {
			case 1 :
				;
			default :
				;
		}
		switch (i) {
			case 1 : case j :
				;
			default :
				;
		}
	}
}